import { Text } from "@components/index";
import { FC } from "react";

interface pageProps {}

const Tasks: FC<pageProps> = ({}) => {
  return <Text size="head">Tasks</Text>;
};

export default Tasks;
