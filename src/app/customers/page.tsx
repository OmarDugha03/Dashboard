import { FC } from "react";
import { Text } from "@components/index";
interface pageProps {}

const Customers: FC<pageProps> = ({}) => {
  return <Text size="head">Customers</Text>;
};

export default Customers;
