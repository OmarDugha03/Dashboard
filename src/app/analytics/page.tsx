import { Text } from "@components/index";
import { FC } from "react";

interface pageProps {}

const Analytics: FC<pageProps> = ({}) => {
  return <Text size="head">Analytics</Text>;
};

export default Analytics;
